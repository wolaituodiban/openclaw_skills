# Review log: local-image-gen

> Append-only record of every artifact that has cleared both gates
> (sub-agent review + human review). Most recent entry at the bottom.
> Format: `<ISO timestamp> | <artifact path> | <sub-agent verdict> | <human verdict> | <one-line note>`

---

## Step 1 — Requirements

| Timestamp | Artifact | Sub-agent | Human | Note |
|-----------|----------|-----------|-------|------|
| 2026-06-28T15:48:00+08:00 | `dev-doc/01-requirements.md` v1 → v2 | PASS — 12/12 v1 issues RESOLVED, 1 minor V2-1 | PASS | Owner ratified 4 design decisions: plaintext bearer token, single-GPU no-multi-GPU-design, no same-model concurrency (each service queues), no reference GPU. v3 simplified single-service invariant to global (regardless of model_id). |
| 2026-06-28T16:01:00+08:00 | `dev-doc/01-requirements.md` v2 → v3 (Gate 2) | (verification pass at v2 in `reviews/01-requirements-review-v2.md`: 12/12 RESOLVED + 1 minor V2-1 circular cross-ref) | PASS | V2-1 fixed by §9 becoming sole authority on model lock, §8 references §9 unidirectionally. v3 single-service wording verified consistent across §3 Goal 3, §4 Non-goals, FR-3, FR-8. |
| 2026-06-28T16:04:00+08:00 | `dev-doc/01-requirements.md` amendment | (amendment, no re-review — minor library name correction discovered when entering step 2) | (REVERSED, see next entry) | Two locations corrected: §1 (line 5) and §9 Glossary (line 77): `diffusers ZImagePipeline` → `modelscope ZImagePipeline`. Was based on the prototype `z_image_turbo_poc.py` content at the time. |
| 2026-06-28T16:14:00+08:00 | `dev-doc/01-requirements.md` amendment (reverse) + `dev-doc/02-architecture.md` rewrite (5 locations + mermaid) | (amendment, no re-review — owner-reported prototype update) | (pending Gate 2 at step 2) | Prototype `~/.openclaw/workspace/z_image_turbo_poc.py` switched from `modelscope.ZImagePipeline` to `diffusers.ZImagePipeline` (with `local_files_only=True` and explicit on-disk model path). Reverted 16:04 amendment; propagated library change to architecture doc §1, §3.1, §5, §6 Tech stack, §7 cross-cutting, §8 risks. Added `LOCAL_IMAGE_GEN_MODEL_PATH` env var (FR-1 path mode) — see architecture doc for new env var list. **Substantive addition**: prototype uses explicit on-disk path + `local_files_only=True`, not a manifest file. FR-1 in step 1 still references the manifest design; this needs explicit alignment discussion at step 2 Gate 2. |
| 2026-06-28T16:25:00+08:00 | `dev-doc/02-architecture.md` §1 mermaid diagram rewrite | (amendment, no re-review — owner-reported mermaid parse error) | (pending Gate 2 at step 2) | Owner reported `Parse error on line 2: ...gent[MCP Client<br/>(agent or skill)] — Expecting 'PS'`. Fix: removed all `<br/>` (mermaid flowchart does not parse `<br/>` as line break in node labels), removed all parentheses from quoted node text (mermaid parser treats `(` as `PS` token after line break). Mermaid now uses single-line quoted labels and simple `\|label\|` edge labels (no slash, no quotes inside). Skill directory structure observed: `~/.openclaw/skills/dashscope-image/` is reference for `SKILL.md + scripts/ + tests/{unit,integration}/` layout; `local-image-gen/` already has `dev-doc/` and will get `SKILL.md + tests/{unit,integration}/` at step 6. No `scripts/` needed (local-image-gen is a daemon skill, not a CLI skill). |
| 2026-06-28T23:48:00+08:00 | `dev-doc/01-requirements.md` v3 → v4 + `dev-doc/02-architecture.md` v1 → v2 (owner-driven restructure: MCP stdio + 3-env-var minimalism + CLI-arg subprocess + hardcoded timeouts) | (pending — sub-agent v2 review to be spawned next) | (pending Gate 2) | Owner direction: "mcp走stdio模式，mcp port直接砍掉。LOCAL_IMAGE_GEN_BEARER_TOKEN和LOCAL_IMAGE_GEN_STATE_DIR保留，其他全部砍掉。将必要的功能保留在调用的参数中。" Combined changes: (1) MCP transport port → stdio; (2) env vars 11 → 3 (BEARER_TOKEN + STATE_DIR + MODEL_PATH); (3) subprocess internal params env vars → CLI args (`--service-id --model-id --model-path --bearer-token --state-dir`); (4) all timeouts hardcoded (start 30 s, invoke 120 s, release grace 10 s, shutdown 30 s); (5) `start_service(model_id, model_path=None)` parameter override of `LOCAL_IMAGE_GEN_MODEL_PATH` env var; (6) FR-7 rewritten (stdio), FR-9 rewritten (stdin EOF / agent exit), NFR-4 (stderr), NFR-5 (no loopback policy), Goal 4/5 (stdio + agent lifetime), §7 Operator (mcp_servers config), §9 Glossary (MCP server stdio); (7) Step 2 §1 mermaid redrawn, §3.1 (CLI args), §3.2 (stdio + stdin EOF shutdown), §4 (CLI args), §5 dir tree (`test_stdio_transport.py` added), §6 (single `local-image-gen-mcp` entry point), §7 env vars table (3 only) + mcp_servers config JSON example, §8 risks (stdio pipe backpressure). Owner also flagged skepticism about `LOCAL_IMAGE_GEN_START_TIMEOUT` 30 s default (deferred to v1 implementation). |
| 2026-06-28T23:54:00+08:00 | `dev-doc/01-requirements.md` v4 → v5 (correction: MCP server started by OpenClaw gateway, not by agent) | (intermediate correction, superseded by 2026-06-29T00:00 entry below) | (pending Gate 2) | Owner correction: "mcp server启动方式不对，mcp server应该由openclaw gateway来启动，gateway活则mcp server活，gateway死则mcp server死。mcp server模块不需要考虑这些，完全托管给openclaw gateway。" Initial attempt: rewrote FR-7/FR-9/§7 Operator/§9 Glossary to remove agent-as-spawner and assign model-server cleanup to the gateway. v5 effective content is superseded by the v6 correction below. |
| 2026-06-29T00:00:00+08:00 | `dev-doc/01-requirements.md` v5 → v6 + `dev-doc/02-architecture.md` v3 (correction: MCP server cleanup delegation — gateway does not reach into model-server) | (pending — sub-agent v3 review to be spawned next) | (pending Gate 2) | Owner correction: "还是不对！正确应该是openclaw gateway 管mcp-server，mcp-server管model-server，openclaw gateway不越级管model-server。" Refined v6/v3 (the actual Gate 2-ready state): lifecycle delegation chain is **gateway → mcp-server → model-server**, each level only managing its direct child. (a) Step 1 FR-9 now correctly says "MCP server is responsible for cleaning up the model-server it spawned; the OpenClaw gateway does not reach into the model-server directly." (b) Step 1 §9 Glossary MCP server entry updated to reflect gateway-owns-mcp-server + mcp-server-owns-model-server. (c) Step 1 Goal 5 corrected (initial v5 wrongly assigned model-server cleanup to gateway). (d) Step 2 §1 mermaid now explicitly shows `Agent → Gateway → MCPServer` relay path (no direct Agent → MCPServer arrow). (e) Step 2 §3.2 corrected: stdin EOF shutdown handler IS in the MCP server (it manages its own child, the model-server), but the MCP server's own lifecycle is owned by the gateway. (f) Step 2 §7 mcp_servers config example moved to `gateway.config.mcp.servers` with nested `mcp.servers.<name>` shape. (g) Step 2 §6 entry point description, §5 README description, §8 risks "zombie" entry — all updated to reflect gateway-managed MCP server + mcp-server-managed model-server. (h) Step 1 NFR-5: "MCP server's tool-call surface is exposed only via the OpenClaw gateway's private stdin/stdout pipe" — agent pipe replaced with gateway pipe. v6/v3 supersedes the v5 intermediate; the v5 23:54 entry remains in the log for audit purposes. |
| 2026-06-29T00:13:00+08:00 | `dev-doc/01-requirements.md` v6 → v7 + `dev-doc/02-architecture.md` v3 → v4 (cache-only model resolution: kill `LOCAL_IMAGE_GEN_MODEL_PATH`, add 5-level cache lookup chain) | (pending — sub-agent v4 review to be spawned next) | (pending Gate 2) | Owner direction: "不要使用LOCAL_IMAGE_GEN_MODEL_PATH，请使用huggingface和modelscope的默认路径和环境变量。model_id就是Tongyi-MAI/Z-Image-Turbo这种标准命名。start_service接受model_id之后，依次从huggingface cache环境变量、huggingface默认cache path、modelscope cache环境变量、modelscope 默认cache path和用户传入的cache_dir中去匹配。" Combined changes: (1) **`LOCAL_IMAGE_GEN_MODEL_PATH` env var removed entirely** — no skill-specific model-path env var anymore. (2) skill-specific env vars reduced from 3 → 2 (BEARER_TOKEN + STATE_DIR only). (3) **5-level cache lookup chain** added to FR-3 / §3.1 / §4 / §7 / §8 / §9 Glossary: HF env var (`HF_HOME` / `HUGGINGFACE_HUB_CACHE`) → HF default (`~/.cache/huggingface/hub/`) → MS env var (`MODELSCOPE_CACHE`) → MS default (`~/.cache/modelscope/hub/`) → user `cache_dir` per-call argument. (4) `start_service` signature changed: `model_path=None` → `cache_dir=None`; new response field `cache_source ∈ {hf_env, hf_default, ms_env, ms_default, cache_dir}`. (5) FR-1 list_local_models rewritten: recognized model id set is hardcoded to `["Tongyi-MAI/Z-Image-Turbo"]` in v1 (single-model); no manifest file. (6) Step 2 §4 subprocess CLI args changed: `--model-path` removed, added `--cache-dir` + `--cache-source`; model-server resolves `--model-id` against `--cache-dir` using the library implied by `--cache-source`. (7) Step 2 §7 mcp_servers config example: MODEL_PATH env var removed from JSON; cache resolution documented as inherited from gateway env. (8) Step 2 §8 risks "model artifact not on disk": error message rewritten to reflect 5-level chain search + suggest both `modelscope download` and `huggingface-cli download`. (9) New Goal 7 added: "Model resolution is cache-only in v1 ... standard library cache env vars and standard library default paths are the only configuration surface for cache roots." (10) Step 2 §3.1 list_local_models description updated: `${LOCAL_IMAGE_GEN_MODEL_PATH}` reference replaced with "model id Tongyi-MAI/Z-Image-Turbo, resolved via the 5-level cache lookup chain". (11) NFR-4 startup logs: removed `LOCAL_IMAGE_GEN_MODEL_PATH` from log fields; added `cache_source` to per-`start_service` log fields. All three remaining `LOCAL_IMAGE_GEN_MODEL_PATH` mentions in step 2 are **deliberately retained as audit-trail documentation of the 2026-06-29 removal** — none describe functional behavior. |
| 2026-06-29T00:24:00+08:00 | `dev-doc/01-requirements.md` v7 → v8 + `dev-doc/02-architecture.md` v4 → v5 (rename `model_id` → `model_name`; `invoke_model` takes `model_name`, not `service_id`) | (pending — sub-agent v5 review to be spawned next) | (pending Gate 2) | Owner direction: "把model_id改成model_name，已保持于hf的明明一致性。同时invoke_model不要service_id，而是传入model_name，保证agent使用便捷。" Combined changes: (1) **Field rename `model_id` → `model_name` across all surfaces** (FR-1 / FR-2 / FR-3 / FR-4 / FR-5 / FR-8 / NFR-4 / §6 Agent / §9 Glossary / §3.1 list_local_models / §3.1 list_running_services / §3.1 start_service response / §3.1 start_service lookup / §3.1 invoke_model signature / §4 subprocess `--model-name` / §1 PID-and-meta file). (2) **Rationale (per Glossary entry):** "model name" is the term HuggingFace uses for repo ids on the hub; aligning with HF naming keeps terminology consistent with the rest of the HF ecosystem. (3) **`invoke_model` signature changed**: removed `service_id` argument; now `invoke_model(model_name, prompt, size?, seed?, steps?)`. The agent passes the model name directly without tracking service ids. (4) **`invoke_model` lookup mechanism documented in §3.1**: MCP server scans `${STATE_DIR}/service-*.json` files, prunes any whose `pid` is not alive, and returns `no_running_service` if zero remain; if one remains, verifies its `model_name` field matches the argument (mismatch → `model_not_loaded`); on match, reads `bearer_token` and `port` from the file and forwards the request. (5) **Why this works (FR-8 clarification):** v1's global single-service invariant means at most one PID-and-meta file exists at any time, so the lookup is effectively O(1) in the common case. The agent never has to track `service_id`s between `start_service` and `invoke_model` calls. (6) **Serialization clarification (§3.1):** the per-service lock is keyed by `service_id` (since locks are per-process not per-model), but the single-service invariant means there's only ever one lock in practice. (7) **NFR-4 invoke_model log fields added**: `model_name` (lookup argument), `service_id` (matched), `cache_source` (from service record). (8) **FR-4 error semantics enriched:** added `no_running_service` and `model_not_loaded` as distinct error cases; clarified that `invoke_model` does NOT auto-start the service (agents must call `start_service` explicitly). (9) **`release_service(service_id)` retains its `service_id` argument** — release is a low-level operation where the agent (or operator) explicitly knows the service id (e.g. obtained from `list_running_services` diagnostics). This is the only `service_id` argument that remains in the v1 MCP tool surface. The only remaining `model_id` mention in step 1 is the Glossary audit-trail annotation "Renamed from `model_id` to `model_name` in 2026-06-29" — deliberately retained for traceability. |
| 2026-06-29T00:35:00+08:00 | `dev-doc/01-requirements.md` v8 → v9 + `dev-doc/02-architecture.md` v5 → v6 (align MCP tool surface with OpenClaw `image_generate`; rename `model_name` → `model`; `release_service` takes `service_id` or `model`) | (pending — sub-agent v6 review to be spawned next) | (pending Gate 2) | Owner direction: "invoke_model的设计请参考openclaw 的image_generate，不急着改，现成请有哪些参数，让我来挑" + "选择prompt，image，images，model，filename，size，outputFormat，count和timeoutMs这几个参数。start_service的model_name也改名叫model。release_service既可以接受service_id，也可以接受model_name。" Combined changes: (1) **Field rename `model_name` → `model` across all surfaces** (FR-1 / FR-2 / FR-3 / FR-4 / FR-5 / FR-8 / NFR-4 / §6 Agent / §9 Glossary / §3.1 list_local_models / §3.1 list_running_services / §3.1 start_service signature + response / §3.1 invoke_model signature / §4 subprocess `--model` / §1 PID-and-meta file). The MCP tool parameter is now `model`; the PID-and-meta file field is `model`; the response shape field is `model`; the CLI arg is `--model`. (2) **`invoke_model` signature expanded to 11 args** to match OpenClaw's built-in `image_generate` tool (the owner explicitly cross-referenced `dist/openclaw-tools-B0V1p3La.js` `ImageGenerateToolSchema` for reference): `invoke_model(prompt, model=None, image=None, images=None, filename=None, size=None, outputFormat="png", count=1, seed=None, steps=None, timeoutMs=None)`. Owner-selected 9 from `image_generate` (`prompt` / `image` / `images` / `model` / `filename` / `size` / `outputFormat` / `count` / `timeoutMs`); **plus 2 diffusion-specific args** (`seed` + `steps`) that are NOT in `image_generate` (which wraps remote APIs that don't accept a seed) but are v1 z-image-turbo features. **`model` is now optional** in `invoke_model` (defaults to whichever service is running under v1's single-service invariant). (3) **`release_service` signature changed to dual-key**: `release_service(service_id=None, model=None)` — exactly one of `service_id` or `model` must be provided; if both, `service_id` wins; if neither, validation error. Lookup mechanism: `service_id` matches the file directly; `model` scans `service-*.json` and verifies the `model` field matches (mismatch → `model_not_loaded`). Symmetric with `invoke_model` lookup. (4) **Step 2 §3 model-server CLI args updated** to match the new naming: `--model-id` (stale) → `--model` (HuggingFace-style repo name); `--model-path` (stale, pre-cache-chain) → `--cache-dir` (directory root) + `--cache-source` (one of `hf_env | hf_default | ms_env | ms_default | cache_dir`); the model-server uses `--cache-source` to decide which library (HuggingFace `diffusers` or ModelScope) and which layout (`models--<org>--<repo>` vs `models/<org>/<repo>`) to load from. v1 z-image-turbo only sees `hf_default | ms_default | cache_dir` as valid sources. (5) **Step 2 §1 top-of-page description updated**: "explicit on-disk model path" (stale pre-cache-chain wording) → "resolved via the 5-level cache lookup chain (FR-3) at `start_service` time". (6) **Step 2 §3.1 model-server module description updated**: "v1 supports Z-Image-Turbo only, loaded via `diffusers.ZImagePipeline` with `local_files_only=True` and an explicit on-disk model path" → "loaded via `diffusers.ZImagePipeline` (or ModelScope's diffusers-compatible API) with `local_files_only=True`, resolved via the 5-level cache lookup chain". (7) **§9 Glossary `model` entry now records the full naming history**: "`model_id` (literal repo id) → `model_name` (HF naming alignment) → `model` (OpenClaw `image_generate` tool naming alignment)". All three legacy names appear in this one audit-trail entry only; no other mention in the docs. **Result: zero `model_id` / `model_name` mentions remain** in the functional spec (step 1 line 19 / 38 / 79 / step 2 line 71 / 82 / 86 / 88 / 92 / 95 / 97 / 99 / 114 — all rewritten to use `model`). |
| 2026-06-29T00:46:00+08:00 | `dev-doc/01-requirements.md` v9 → v10 + `dev-doc/02-architecture.md` v6 → v7 (`start_service` adds `timeoutMs` per-call override; NFR-2 deleted as duplicate; `invoke_model` drops `seed` and `steps`; FR-8 fails-fast `service_already_running`) | (pending — sub-agent v7 review to be spawned next) | (pending Gate 2) | Owner direction: "start_service加入timeoutMs参数。NFR-2与start_service timeoutMs重复需要删除。invoke_model删除seed和steps。FR-8如果当前已经有model server，start_service失败且返回原因。" Combined changes: (1) **`start_service` signature gains `timeoutMs`**: `start_service(model, cache_dir=None, timeoutMs=None)`. When supplied and > 0, overrides the hardcoded default 30 s start timeout for this single call. Default 30 s applies when omitted. Enforced on the MCP server's poll of `/health`; on timeout, subprocess is terminated and no PID-and-meta file is written; returns `start_timeout`. (2) **NFR-2 deleted**: previously "Service startup latency — `start_service` returns within 30 s"; now subsumed by FR-3. Two audit-trail mentions in step 2 §3 / §7 cross-cutting decisions note "NFR-2 was removed in 2026-06-29 since the start-timeout guarantee is now part of FR-3". (3) **`invoke_model` drops `seed` and `steps`**: signature now `invoke_model(prompt, model=None, image=None, images=None, filename=None, size=None, outputFormat="png", count=1, timeoutMs=None)` — 9 args exactly matching `image_generate`. The diffusers / OpenAI HTTP endpoint still accepts `seed` at the model-server HTTP layer (FR-6 / §3 `POST /v1/images/generations` retains `seed?` for compatibility with the OpenAI spec) but the MCP `invoke_model` surface does NOT surface it — callers cannot override the model's internal defaults in v1. (4) **FR-8 fails-fast changed**: when a model-service is already running, `start_service` now fails with `service_already_running` (instead of returning the existing record). Error message surfaces the existing service's `model` and `service_id` so the caller can decide whether to invoke the running service or call `release_service` first. The MCP server does not spawn a new process and does not auto-release the existing one. (5) **Step 2 §3 / §4 subprocess CLI args gain `--timeout-ms`**: `--service-id --model --cache-dir --cache-source --bearer-token --state-dir --timeout-ms`. The model-server uses `--timeout-ms` as its own defensive bound for `/health` reporting ready (second-layer bound independent of the MCP server's own polling timeout); a misbehaving model-server that exceeds the bound self-aborts with a non-zero exit. (6) **§9 Glossary gains `timeoutMs` and `service_already_running` entries**. (7) **§6 Agent description updated** to mention `service_already_running` handling: agent decides whether to invoke the running service or call `release_service` first. (8) **§8 risks row about slow `ZImagePipeline` warm-up** updated to reflect per-call override behavior. **Verification:** `grep NFR-2 step1 step2` returns 3 audit-trail mentions only (all saying "NFR-2 was removed"); `grep seed/steps` returns only the FR-6 / §3 HTTP layer references and the §3.1 / §4 / FR-4 "intentionally NOT exposed" annotations; `invoke_model` signature is identical across step 1 and step 2 (9 args); `start_service` signature is identical across step 1 and step 2 (3 args); `--timeout-ms` is consistent across §3 model-server args + §4 subprocess launch. |
| 2026-06-29T01:01:00+08:00 | `dev-doc/02-architecture.md` v7 → v8 (mcp_server uses `openai.OpenAI` sync client instead of hand-rolled `httpx` to talk to model-server) | (pending — sub-agent v8 review to be spawned next) | (pending Gate 2) | Owner direction: "step2，mcp_server使用openai client连接model server是不是更好？" + "换openai client，现在就改". Rationale: model-server exposes an OpenAI-compatible Images API (FR-6), so the official `openai` Python client is more idiomatic than hand-rolled `httpx` POSTs — handles `base_url`, `Authorization: Bearer <token>`, request body, and error surfacing for free. Bonus: drops one explicit dependency (MCP server no longer imports `httpx` directly; it comes in transitively via the `openai` package). Combined changes: (1) **§3.1 mcp_server dependencies row rewritten**: `httpx (sync client)` → `openai.OpenAI` client (sync mode). Configured with `base_url=http://127.0.0.1:<port>/v1` and `api_key=<bearer-token-read-from-PID-and-meta-file>`. Note appended that `httpx` is a transitive dep of `openai` (MCP server does not import it directly). (2) **§6 Tech stack HTTP client row updated**: choice → `openai.OpenAI` (sync mode); rationale expanded to mention base_url / Authorization / request body / error surfacing handled by the client. (3) **§6 Tech stack test framework row updated**: `pytest + httpx + pytest-asyncio` → `pytest + pytest-asyncio`; parenthetical notes that tests talk to the MCP server via stdio JSON-RPC and exercise the model-server via FastAPI's `TestClient`, so `httpx` is no longer an explicit dev dep. (4) **§6 Tech stack gains a Runtime deps row** explicitly listing `fastapi` + `uvicorn` + `diffusers` + `torch` + `mcp` (FastMCP) + `openai` + stdlib — so the dependency story is captured in one place (otherwise it would have been implicit from the per-module descriptions). (5) **§5 integration test description for `test_global_single_service.py` updated** to match FR-8 fails-fast semantics: "second `start_service` returns `service_already_running` and surfaces the existing service's `model` + `service_id`" — the prior wording ("returns existing") described the pre-2026-06-29-00:46 idempotent-ish behavior that has since been replaced. **Verification:** `grep httpx step2` returns 4 mentions, all audit-trail / explanatory (line 130 transitive / line 173 "instead of httpx" / line 176 "no longer explicit" / line 179 runtime deps); `openai.OpenAI` appears in 4 functional spots (line 130 / 173 / 176 / 179); the model-server's OpenAI-compatible API description (line 48 / 57 / 61 / 184) is unchanged because it refers to the API *form* (the wire protocol), not to the client library. File size: step 1 unchanged at 26787 bytes / 89 行; step 2 v7 → v8 at 34092 bytes / 222 行 (+1557 bytes / +1 行 from the dependency description additions). |
| 2026-06-29T01:13:00+08:00 | `dev-doc/01-requirements.md` v10 → v11 + `dev-doc/02-architecture.md` v8 → v9 (split 5-level cache lookup chain responsibility: model-server is authoritative resolver; `cache_source` written by model-server to PID-and-meta file; `--cache-source` CLI arg dropped) | (pending — sub-agent v9 review to be spawned next) | (pending Gate 2) | Owner direction: "local_image_gen.model_server删掉--cache-source，按照hf_env \| hf_default \| ms_env \| ms_default \| cache_dir的顺序进行路径解析。问题：start timeout到底是30s硬编码？还是由local_image_gen.model_server --timeout-ms决定？还是由tart_service(timeoutMs=...)决定？" + "--cache-source按照A来改". Owner chose solution A: the model-server is the authoritative resolver; `cache_source` is written by the model-server to the PID-and-meta file and read back by the MCP server (single source of truth). The MCP server does a lightweight existence pre-check over the 5 levels only to surface `model_not_found` before spawning. Combined changes: (1) **step 1 FR-3 rewritten**: previously said "the MCP server resolves `model` to an on-disk snapshot via the 5-level chain"; now says "the model-server is the authoritative resolver of the 5-level cache lookup chain — it walks the chain itself at startup and stops at the first level that contains the requested model"; the MCP server's role is reduced to "a lightweight existence pre-check (file-system walk over the 5 levels) only to surface `model_not_found` before spawning". (2) **step 1 §1 Goal 7 appended** with a one-sentence callout: "The model-server is the authoritative resolver of the 5-level chain (FR-3 / step 2 §3); the MCP server does a lightweight existence pre-check only ... The `cache_source` field on the service record is written by the model-server to the PID-and-meta file and read back by the MCP server — single source of truth." (3) **step 1 §9 Glossary `5-level cache lookup chain` entry updated**: rephrased from "the fixed order in which `start_service` searches" to "the fixed order in which the **model-server** (authoritative resolver) searches for an on-disk snapshot of `model` at startup"; appended that the model-server writes `cache_source` to the PID-and-meta file (single source of truth) and the MCP server does a lightweight pre-check. (4) **step 2 §3.1 mcp_server `start_service` rewritten**: dropped the "MCP server resolves the chain" bullet; replaced with "Performs a lightweight existence pre-check over the 5-level cache lookup chain (FR-3): walks the same 5 levels the model-server will walk at startup ... If none of the 5 levels contains the requested model, returns `model_not_found` immediately without spawning"; appended that the response `cache_source` is "read from the PID-and-meta file (where the model-server wrote it) — the MCP server never re-derives it". (5) **step 2 §3 model-server module `Purpose` paragraph** got a new first sentence: "Authoritative resolver of the 5-level cache lookup chain (FR-3) — the model-server walks the chain itself at startup, decides which level contains the model, and writes the winning level as `cache_source` into the PID-and-meta file." (6) **step 2 §3 model-server CLI args list updated**: **dropped the `--cache-source` row entirely**; appended a separate negative bullet "There is no `--cache-source` CLI arg — the model-server resolves the 5-level cache lookup chain itself at startup ..." explaining why (single source of truth). (7) **step 2 §3 model-server PID-and-meta file shape updated**: was `{service_id, pid, port, model, started_at, bearer_token}` → now `{service_id, pid, port, model, started_at, bearer_token, cache_source}`; with note "`cache_source` is the winning level of the 5-level cache lookup chain ... the model-server is the authoritative resolver and writes this field itself". (8) **step 2 §3.2 mcp_server startup behavior updated**: was "validates the cache lookup chain (walks all 5 levels and records which level holds the recognized model names, for diagnostics; the MCP server does **not** fail-fast if no level contains a model name)"; now emphasizes "The walk on startup is a **read-only directory scan** (it does not load any model and does not bind any decision about which level will win); the model-server is the authoritative resolver at `start_service` time". (9) **step 2 §4 subprocess launch paragraph updated**: removed `--cache-source <hf_env|hf_default|ms_env|ms_default|cache_dir>` from the CLI args list; appended "There is no `--cache-source` arg — the model-server walks the 5-level chain itself and decides which library ... and which layout ... to use"; appended "The PID-and-meta file includes `cache_source` (the winning level), which the MCP server reads back to construct the `start_service` response". (10) **step 2 §8 risks `model artifact not on disk` row updated**: now describes both error paths — the MCP server's pre-spawn existence walk (immediate `model_not_found`) and the model-server's resolution failure (subprocess exits non-zero with stderr error). **Timeout clarification (also part of this amendment):** the owner asked which of the three layers (hardcoded 30 s / `--timeout-ms` CLI / `start_service(timeoutMs=...)` parameter) is authoritative. The answer: (a) **`start_service(timeoutMs=...)`** is the user-facing per-call override parameter at the MCP tool surface (FR-3); default 30 s. (b) **model-server `--timeout-ms`** is a second-layer defensive bound (model-server self-aborts on startup past this bound); the MCP server forwards `timeoutMs` to it via CLI arg. (c) **30 s hardcoded default** applies only when `timeoutMs` is omitted from `start_service`. The two layers are not in conflict; they are nested — the outer `start_service(timeoutMs=...)` is enforced by the MCP server's poll of `/health`, the inner `--timeout-ms` is the model-server's own bound. Both default to 30 s when no override is supplied. **Verification:** `grep "\-\-cache-source" step1` returns no matches; `grep "\-\-cache-source" step2` returns 3 matches, all negative ("There is no `--cache-source` CLI arg" / "the MCP server does NOT pass a `cache_source` value (there is no `--cache-source` CLI arg)"); `grep "authoritative resolver" step1+step2` returns 6 matches across both files (consistent use); CLI args list at step 2 §3 line 65-71 has 6 args only (`--service-id --model --cache-dir --timeout-ms --bearer-token --state-dir`); PID-and-meta file shape at step 2 §3 line 73 now includes `cache_source`. File size: step 1 v10 → v11 at 28288 bytes / 89 行 (+1501 bytes from FR-3 / §1 / §9 expansions); step 2 v8 → v9 at 36328 bytes / 222 行 (+2236 bytes from §3 purpose / CLI args / PID-and-meta / §3.1 start_service / §4 subprocess / §3.2 startup / §8 risks). |
| 2026-06-30T22:04:00+08:00 | `dev-doc/01-requirements.md` v11 → v12.1 (merged) + `dev-doc/02-architecture.md` v9 → v3 (rewritten from scratch for v12.1) | (pending — sub-agent v12.1 review to be spawned next) | (pending Gate 2) | **Two-step transition (v12 drafted, then POC-validated amendments folded into v12.1):** (1) v11 → v12 (2026-06-30, owner direction): replace the in-skill `model_server.py` FastAPI harness with **vllm-omni as the only model-server backend**. The MCP server launches `vllm serve --omni <absolute-snapshot-path> --port <port> --api-key <bearer_token>` as a subprocess; the cache lookup chain becomes the **MCP server's authoritative resolver** (was the model-server's job in v11), and the absolute path is passed as the positional `<model>` argument. Modelscope bridging is then POC-gated (§8 Open Questions). (2) POC against vllm-omni 0.22.0 + Z-Image-Turbo at `~/.cache/modelscope/hub/models/Tongyi-MAI/Z-Image-Turbo` (2026-06-30T22:00, ~10 min wheel compile): **scheme B confirmed** — `vllm serve --omni` accepts the standard ModelScope layout `models/<org>/<repo>/` directly, auto-detects `model_index.json` at the root, and dispatches `ZImagePipeline`. **`--api-key` confirmed** — vllm upstream wires it into a FastAPI `AuthenticationMiddleware` that protects every route. **`/v1/models` readiness probe confirmed** — vllm-omni 0.22.0 overrides upstream `/v1/models` with an omni-aware handler at `entrypoints/openai/api_server.py:452` that returns the loaded model card once the diffusion engine reports ready. **`/v1/images/edits` multipart field names confirmed** — `image`, `image[]` (alias `image_array`), `mask_image`, `reference_image`, `prompt`, `model`, `n`, `size`, `response_format`, `output_format`, `negative_prompt`, `num_inference_steps`, `guidance_scale`, `strength`, `seed`, `lora`. **vllm-omni `--omni` flag hidden from `--help=all`** but present in `OmniConfig` and the vllm-omni CLI argparse; required for diffusion mode. POC blocked on full end-to-end image generation by missing `python3-devel` in the WSL env (JIT compile of `cuda_utils.c` fails during the post-`Worker ready` dummy warm-up); independent of scheme B correctness — model load + worker init + source verification already validate the architecture. **v12 → v12.1 amendments folded in (2026-06-30T22:04, owner approved all three):** (a) **Drop `service_id` entirely** — under the single-service invariant (FR-8) it was redundant. PID-and-meta file is renamed to a fixed `${STATE_DIR}/service.json`; `start_service` response shape no longer carries a `service_id` field; `release_service` accepts `model=` only (matches the v11 surface, simpler for v1). `list_running_services` returns 0 or 1 entries (no `service_id` field). When v2 reintroduces multi-service, `service_id` will return as a first-class field on this surface. (b) **Drop mcp_server FIFO lock** — vllm-omni's `omni_run_server` is documented as a single-worker API server (source: `vllm_omni/entrypoints/openai/api_server.py:396`, uvicorn workers default to 1, FastAPI async handlers queue FIFO within the single worker). The MCP server does **not** hold a per-service request lock and does **not** serialize calls client-side — it forwards each `invoke_model` to vllm-omni as an independent request and lets the upstream server queue. (c) **Drop `--api-key` fallback** — `--api-key` is unconditionally supported (POC-verified + source-verified at `vllm/entrypoints/openai/api_server.py:265-269`); there is no "endpoint left unauthenticated" fallback path. `--host 127.0.0.1` is still passed defensively (defense in depth) but is not a substitute for `--api-key`. **Combined step 1 changes:** status header rewritten to v12.1 with POC status block; FR-3 expanded (auth paragraph + model_path field + scheme B note); FR-4 FIFO sentence removed; FR-5 changed to `release_service(model)`; FR-8 v12.1 simplification paragraph added; NFR-4 log fields updated to use `model + pid + port` instead of `service_id`; NFR-8 source-cites `entrypoints/openai/api_server.py:452`; §6 Agent description updated (no `service_id` for invoke / release); §8 Open Questions updated with three resolutions (Modelscope path → RESOLVED scheme B; `--api-key` → RESOLVED; readiness probe → narrowed to `/v1/models` but POC calibration still pending; cold-start latency → still POC, observed ~22 s for weight load on RTX 3090, final calibration requires `sudo dnf install python3-devel`); §9 Glossary rewritten for `service_id` (removed) and `PID-and-meta file` (now `${STATE_DIR}/service.json` with `model_path` field). **Combined step 2 changes:** full rewrite from scratch — v9 was the in-skill FastAPI model-server architecture, v3 is the vllm-omni subprocess architecture. §0 changelog table added (v11/v2 vs v12 vs v12.1 across 10 dimensions); §1 mermaid redrawn (mcp_server → vllm-omni subprocess via subprocess.Popen, no in-process FastAPI); §2 module list updated (`model_server.py` deleted, `cache_resolver.py` added as a pure helper); §3.1 mcp_server rewritten in full (cache_resolver call, port-picking trick, vllm-omni CLI args, multipart routing, dual-key lookup removed from `release_service`, no client-side lock); §3.2 cache_resolver module description added; §4 inter-module connections rewritten (subprocess CLI args now include `--omni --api-key --host 127.0.0.1`; bearer token flow rewired through `--api-key` → `AuthenticationMiddleware`); §5 project directory tree updated (no `model_server.py`; new `cache_resolver.py`; new `test_cache_resolver.py` + `test_invoke_routing.py`); §6 Tech stack rewritten (model-server row → vllm-omni; multipart construction row added; runtime deps row simplified to `mcp` + `openai` + stdlib); §7 cross-cutting decisions rewritten (auth flow now `--api-key`; serialization note says "no client-side lock"); §8 risks rewritten (cold-start row reflects vllm-omni instead of diffusers direct; ABI-drift risk row added; `--omni`-flag-hidden-from-help row added). **Verification:** `grep "service_id" step1` returns 8 matches — all in v12.1 explanatory text (status header bullet / FR-3 explanation / FR-8 v12.1 note / PID-and-meta glossary / Glossary entry / etc.); zero live API-contract references remain. `grep "service_id" step2` returns 5 matches — all in the v12 changelog table or "v12.1: no service_id" notes; zero live API-contract references remain. `grep "model_server.py" step2` returns 4 matches — all in the v12.1 changelog / "deleted" notes; zero live project-tree references remain. `grep "threading.Lock" step2` returns 2 matches — both in the v12.1 changelog / "deleted" notes. File size: step 1 v11 → v12.1 at 30106 bytes / 109 行 (+1318 bytes / +20 行 from the POC status block + amendment explanations + Glossary expansion); step 2 v9 → v3 at 44288 bytes / 268 行 (+7960 bytes / +46 行 from the full rewrite — new §0 changelog, expanded §3.1 mcp_server, new §3.2 cache_resolver, expanded §4 / §5 / §6 / §7, expanded §8 risks). |

| 2026-06-30T22:20:00+08:00 | `dev-doc/01-requirements.md` v12.1 → v12.1-trim ("state what, not how") + `dev-doc/02-architecture.md` §7 cross-cutting (log-fields elaboration) | (pending — sub-agent cross-review to be spawned after this) | (pending Gate 2) | **Docs trim per owner request 2026-06-30T22:18:** owner noted that `01-requirements.md` had grown to 110 lines with significant implementation-detail content (5-level cache chain enumerated in FR-3, CLI args spelled out, PID-and-meta file schema, multipart field names, `outputFormat` mappings, etc.) — inconsistent with `dev-doc-driven` skill convention ("State what, not how. Functional requirements, non-functional requirements, scope, acceptance criteria. **Stop here. Do not think about modules yet.**" Example standard is 65 lines). Owner approved "直接同意" (option 1: rewrite now per plan). **Changes to `01-requirements.md` (110 → 104 lines, 16 420 bytes):** (a) **status header** trimmed from 11 lines → 5 lines (POC status block, 3-decisions-amendment bullet list); pulled into a **§1 "Reference snapshot"** table instead so the meta facts are preserved but compressed (version, amendment date, amendment drivers, reference fixture, pointer-to-impl-detail). (b) **§4 Non-goals** rewritten — removed the previous "vllm-omni 0.14.0+ FLAG_VALUE_DEFAULT 120 s" specific implementation references; remaining non-goals are pure behavioral statements ("Network download", "response_format: url", "Multi-service", "Bearer-token rotation"). (c) **FR-1** shrunk 8 → 4 lines (description + `current_load_status` enum + no-allow-list rule + response shape; removed expansion on model-name format and the "shipped reference fixture" footnote). (d) **FR-2** shrunk 5 → 4 lines (status enum + stale-pruning + response shape). (e) **FR-3** shrunk **80+ → 5 lines** (the API signature + 4 acceptance steps: resolve → spawn → wait → return). The full 5-level chain enumeration, Modelscope bridging POC summary, `cache_source` enum, CLI args, --omni / --api-key source citations, pid-and-meta schema, and model_path audit field **all moved to `02-architecture.md` §3.1 `start_service` block** (already had most of this since the v9 → v3 rewrite). (f) **FR-4** shrunk **40+ → 5 lines** (routing rule + status checks + val-error + diffusion-knobs-not-exposed). Multipart field name lists, `size` / `outputFormat` / `count` value mappings, FIFO source citation **all live in `02-architecture.md` §3.1 `invoke_model` block**. (g) **FR-5** shrunk 7 → 4 lines (no longer enumerates exact signal types, hardcoded timeout values, or response-field list). (h) **FR-6** shrunk 6 → 3 lines (endpoint shape + bearer + b64_json-only — bare-minimum FR description). (i) **NFR-4** shrunk 5 → 4 lines (general description + per-event list moved to 02 §7. (j) **NFR-5** shrunk 6 → 4 lines (the previous "if vllm-omni does not support --api-key, fallback to loopback bind" branch is now deleted — NFR-5 is unconditional). (k) **NFR-8** shrunk 6 → 3 lines (description only; exact endpoint + shape + cadence **cited to `02-architecture.md` §3.1**). (l) **§8 Open Questions** preserved in full — this is the POC-gate tracker, status banners are intentional. (m) **§9 Glossary** trimmed from 13 → 6 terms: kept `Cache source`, `PID-and-meta file`, `Reference fixture`, `service_already_running`, `HuggingFace layout`, `ModelScope layout`. **Deleted terms:** `Service id` (moved to v12.1 explanatory inline note in FR-8 + status header / changelog); `Stale PID-and-meta file` (folded into PID-and-meta definition); `Functional requirement` / `Non-functional requirement` / `MCP` / `timeoutMs` / `OpenAI Images API` (generic terms — dropped to reduce noise; the table contents above carry the same info). **Cross-pointer wiring:** FR-3 / FR-4 / NFR-4 / NFR-8 explicitly cite `02-architecture.md` §X for the implementation specifics they elide. The §5 preamble to FR table makes the contract explicit: "Each FR states what the system does, not how. Wire formats, file layouts, command-line arguments, and protocol field names belong in 02-architecture.md". **Change to `02-architecture.md` (274 → 279 lines, +6 lines, +5 lines added under §7 Logging bullet):** expanded the existing "Logging (NFR-4)" bullet with the **per-event log field lists** that previously lived in `01-requirements.md` NFR-4: (a) **Startup logs** include resolved `LOCAL_IMAGE_GEN_STATE_DIR`, bearer-token-source (`env` / `auto-generated`), 5-level cache lookup order (resolved roots + layouts). (b) **Per-`start_service`** include resolved `model`, resolved `model_path`, requested `timeoutMs` (or hardcoded default when omitted), `cache_source`. (c) **Per-`invoke_model`** include `model` (lookup argument), matched service record's `model + pid + port` (from `service.json`), `cache_source`, routed endpoint (generations vs edits), vllm-omni HTTP status / first error line. (d) **Per-`release_service`** include requested `model`, resolved service record's `model + pid` on success, release duration / signal used. (e) **Per-`list_local_models` / `list_running_services`** include returned count + (for running list) matched `model + pid + status`. **Cross-grep verification post-trim:** `grep "service_id\|model_server.py\|threading.Lock" 01-requirements.md` → 2 matches, both in v12.1 explanatory text (status header bullet enumerating the 3 deleted items; FR-8 future-v2 note). Zero live API-contract references. `grep "service_id\|model_server.py\|threading.Lock" 02-architecture.md` → 12 matches, all in v12 → v12.1 changelog table / status header / `start_service` / `list_running_services` / `invoke_model` / `release_service` / §7 cross-cutting "no client-side lock" explanatory notes. Zero live v12.1 API-contract references (all are flagged "v12.1: no service_id field" type explanations). **Owner follow-up pending:** owner asked the trim question at 22:18; said "直接同意" at 22:19. After this trim, owner-visible-state is docs-only — no code changed. **Next step:** sub-agent cross-review on the trimmed `01-requirements.md` + the targeted `02-architecture.md` §7 addition (or full re-review if owner wants); then proceed to `03-test-strategy.md` per the planned doc chain. |
| 2026-07-01T00:46:00+08:00 | `dev-doc/cache_resolver/03-design.md` (initial draft) | PASS — 8 §-headings match example; per-function shape OK; 6 MINOR (M1 = spec divergence `walk_levels` argless per 02 §3.2, M2 = explicit Parameters paragraph, M3 = `CacheSource` named literal, M4 = `CacheLevel.name` typed as `CacheSource` not `str`, M5 = §4 should be bullet list per example, M6 = Q3 L5 `layout='hf'` nominal must stay flagged as Open) | (deferred) | First time sub-agent review is **actually run** end-to-end on this project (all prior entries marked "(pending — sub-agent review to be spawned next)" without ever spawning). Initial draft was 190 lines with self-invented 8 §-headings (did not mirror example schema); sub-agent caught all structural + spec divergences. Verdict PASS = "no BLOCKER/MAJOR, only MINORs to fix" — the fix pass is the next entry. Owner directive 2026-07-01T00:38: "02 定版了，不需要 sub-agent review" applied only to the 01/02 docs that had been iterated 12+ times; for **new artifacts** (03/04/05/06) sub-agent review is the **Gate 1** per the dev-doc-driven skill, not bypassed. |
| 2026-07-01T00:48:00+08:00 | `dev-doc/cache_resolver/03-design.md` v0 → v1 (6 MINOR fixes) | PASS — R2 verified M1–M6 all RESOLVED; 8 §-headings intact; per-function shape intact; spec interface aligned (`walk_levels()` argless per 02 §3.2, `resolve(model, cache_dir=None)` carries L5, `CacheSource` Literal alias introduced); 1 new MINOR (R2-found) — L5 `snapshot_for = _cache_dir_snapshot` has signature mismatch (helper takes `(cache_dir, model)`, field expects `(model,)`); needs `functools.partial` | (deferred) | Edits: (a) M1 `walk_levels` dropped `cache_dir` param, (b) M2 added `**Parameters:** none.`, (c) M3+M4 introduced `CacheSource = Literal['hf_env', 'hf_default', 'ms_env', 'ms_default', 'cache_dir']` at top, applied to `CacheLevel.name` and `resolve` return, (d) M5 §4 rewritten as bullet list (5 bullets: 3 helpers + 2 constants), (e) M6 Q3 retained as Open with status "acceptable per 02 §3.2 ... but the field is effectively unused for L5". File: 128 → 106 lines (-22 lines). R2 caught a real spec adherence issue (signature shape) — confirms the sub-agent review loop is doing its job, not rubber-stamping. **Verdict logic moved to agent** (per owner pushback 2026-07-01T00:49): sub-agent verdicts are PASS/FAIL on objective criteria (M-N resolved, no new BLOCKER/MAJOR); agent decides verdict action (re-draft + R3 / accept + log / etc.); user is Gate 2 (ship / reject / amend), not Gate 1. |
| 2026-07-01T00:51:00+08:00 | `dev-doc/cache_resolver/03-design.md` v1 → v2 (R2 MINOR fix: `functools.partial` closure) | (rolled into above — last edit was post-R2 verdict before this log entry; sub-agent R2 verdict was PASS, agent fixed the one MINOR it found, file is v2 now) | (pending Gate 2) | Single edit pass: (a) §3.2 behavior step 1 — `snapshot_for = _cache_dir_snapshot` → `snapshot_for = functools.partial(_cache_dir_snapshot, cache_dir)`, with parenthetical explaining the partial bridges the 2-arg helper into the 1-arg `Callable[[str], Optional[str]]` field shape. (b) §6 Dependencies — added `functools.partial` to the external libraries list (still stdlib). File 106 lines unchanged. **Gate 2 (human) scope for this artifact:** (1) ship 03-design as-is and proceed to 04-test-design; (2) ship with amendments to Q1/Q2/Q3/Q4 open questions; (3) reject and re-draft; (4) Q3 (`layout='hf'` nominal for L5) is the most material open question — human should decide before 04-test-design proceeds since the test surface depends on whether `layout` is a public field for L5 or not. |
| 2026-07-01T00:53:30+08:00 | (user: 03-design 通过 → 04-test-design) | — | (Gate 2 PASS for 03) | User approved 03-design v2 as Gate 2 with all 4 open questions left as-is. Proceeding to 04-test-design. R2 MINOR (closure form) was intentionally not edited in 03 — the design describes behavior, not implementation form, so the closure detail is a 04 concern (test surface confirms the correct behavior, not the exact form of the callable). |
| 2026-07-01T00:54:00+08:00 | — | `dev-doc/cache_resolver/04-test-design.md` v0 (initial draft) | (pending R1) | Initial draft: 156 lines. Schema mirrors `assets/examples/04-test-design.md` 8 §-headings. **§4 deviation from example:** example has "Integration tests — per cross-module flow"; 04 has "Internal helpers (cross-reference)" — this is the "OR equivalent" branch from the R1 brief, justified by 02-architecture.md §3.2 "cache_resolver is a leaf module (no dependencies)". §7 "What is intentionally not tested" explicitly notes "No integration tests". **3 public functions fully covered** in §3: `CacheLevel` (construction × 3, frozen × 1, identity × 1 = 5 tests), `resolve` (L1-L5 hits × 6, L5-omitted × 1, first-hit-wins × 1, all-miss × 2, type-safety × 1 = 11 tests), `walk_levels` (length × 2, L1 × 4, L2 × 1, L3 × 2, L4 × 1, layout × 2, ordering × 1, env-re-read × 1 = 14 tests). **Internal helpers §4** in closed-form whitebox form: `_hf_snapshot` × 4, `_ms_snapshot` × 3, `_cache_dir_snapshot` × 4. **Q1-Q4 carry-through in §8** with `xfail(strict=True)` markers where test behavior depends. R2 closure form acknowledged in §7 "What is intentionally not tested" paragraph — closure form is not whitebox-tested; behavior is tested through `resolve`. |
| 2026-07-01T00:55:30+08:00 | — | `dev-doc/cache_resolver/04-test-design.md` v0 → v1 (2 MINOR fixes from R1) | (agent self-PASS, no R2 spawned) | R1 verdict: PASS with 2 MINOR. **M1** — §8 Q1 entry referenced only `test_walk_levels_l1_present_when_hf_home_set`; both that test AND `test_walk_levels_l1_present_when_huggingface_hub_cache_set` are Q1-dependent (both assert `str(tmp) + '/hub'`). Fix: added xfail annotation to second test (line 85) and updated §8 Q1 entry to reference both. **M2** — `test_walk_levels_l1_hf_home_takes_precedence` description said "Q1 Open question currently" but the test is about precedence ordering, not the `hub/` suffix question. Precedence is locked in 02 §3.2 "Resolution rules" — not an Open question. Fix: rewrote test description to clarify "Precedence is locked in 02-architecture.md §3.2 ... not an Open question. Independent of Q1." File 156 lines unchanged. **Agent self-PASS rationale:** (a) both MINORs are documentation-completeness in test description / cross-reference, not missing tests or schema defects; (b) R1 explicitly found 0 BLOCKER / 0 MAJOR; (c) re-spawning R2 to verify two description-text edits is overhead with no signal. **Gate 2 (human) scope for this artifact:** (1) ship 04-test-design as-is and proceed to 05-dev-plan; (2) ship with amendments to Q1/Q2/Q4 04-mapped test behavior (Q3 resolved in 04 §8 as "no test impact"); (3) reject and re-draft; (4) Q1 is the most material — the `hub/` suffix affects 2 L1 tests. |
| 2026-07-01T01:02:00+08:00 | — | `dev-doc/05-dev-plan.md` v0 (initial draft) | (pending R1) | Initial draft: 71 lines. Schema mirrors `assets/examples/05-dev-plan.md` 5 §-headings + §6 supplementary block on dev-doc-driven skill rules (rule #1 no-code-before-doc-chain, rule #2 sub-agent → human two-stage review, rule #8 progress mirror to `memory/dev-doc-progress/`). **Module order:** cache_resolver (module 1, no deps per 02 §3.2) → mcp_server (module 2, depends on module 1 per 02 §3.1). Note: 02 §2 enumerates them 1=mcp_server / 2=cache_resolver (table-order), but 05 inverts that for **implementation order** (leaf first, consumer second). §1 prose explains the inversion. **Per-module exit criteria:** universal 6-bullet list (source files / unit tests / integration tests / coverage targets / sub-agent review / human review) with module-specific elaborations. **Review log:** Module 1 = 2 rows (source + unit test); Module 2 = 3 rows (source + unit test + integration test). **Risks:** 5 entries (module 1 spec drift / vllm-omni cold-start timeout / Q1-Q4 Open questions / python3-devel missing / NFR-4 log field list). **Done criteria:** 6 entries (test suite green / no open Q / integration smoke / README / 01-04 Gate 2 PASS / human sign-off). Test count claim "30+" for cache_resolver is conservative (actual: 41 = 5 + 11 + 14 + 11). |
| 2026-07-01T01:06:30+08:00 | — | `dev-doc/05-dev-plan.md` v0 R1 verdict | (agent self-PASS, no R2 spawned) | R1 verdict: **PASS, 0 issues, 0 MINOR**. R1 explicitly stated "Ready for human review". Verified against R1's claims: (a) all 02/03/04 section references check out (02 §3.1 mcp_server consumer, 02 §3.2 cache_resolver leaf, 03 §8 Q1-Q4, 04 §8 xfail markers, 01 §8 NFR-4 / FR-3 / 01 §8 cold-start POC); (b) test count 41 is correctly described as "30+"; (c) §6 supplementary block accepted as skill-rule traceability add. **No R2 spawned:** R1 explicit 0-issue + 0-MINOR = no regression risk; respawn would be overhead with no signal. **Owner follow-up (2026-07-01T01:05, user pushback):** owner flagged that 05 lists `mcp_server` as module 2 but no `mcp_server/03-design.md` or `mcp_server/04-test-design.md` exists. This is a **doc-completeness gap**, not a 05-design defect — the 05 v0 row above already noted "mcp_server 03 + 04 will be written before module 2 starts". Per skill rule #1 ("no code before full doc chain approved"), module 2's 03 + 04 must be PASS-gated before module 2 code is written; this is the next work item, not a 05 amendment. **Gate 2 (human) scope for 05:** (1) ship 05 as-is and proceed to write `mcp_server/03-design.md` + `04-test-design.md`; (2) ship 05 with the doc-status column added to the §1 table (more conservative, makes the gap visible at-a-glance); (3) reject and re-draft 05 to defer mcp_server to a separate 05 file. |
| 2026-07-01T01:14:30+08:00 | — | `mcp_server/03-design.md` v0 → v1 amend | (R1 verdict being applied; 04 R1 review in flight at amend time) | Owner audit 2026-07-01T01:14 — `python-coding-rules` §5 (no try blocks) and §8 (unittest only) violations in mcp_server 03 v0 + 04 v0. **§5 violation in 03 v0:** §3.1 / §3.3 / §3.4 "Raises" sections claimed "I/O errors caught and treated as partial / no service" — internal-code try-wrapping forbidden. **§5 fix in 03 v1:** each tool's "Raises" section now names concrete exception classes (`OSError`, `PermissionError`, `json.JSONDecodeError`, `cache_resolver.CacheResolverError`, `openai.OpenAIError`, `base64.binascii.Error`, `subprocess.SubprocessError`, `FileNotFoundError`); behavior steps that previously said "I/O errors caught and treated as X" now say "**if exception, propagate to framework**" and clarify that the FastMCP framework's request handler is the boundary (§5: "Catch only at the boundary"). Stale-PID cleanup is NOT an exception path (it is a routine write); the relevant text is preserved with clarification. **§8 violation in 04 v0:** §2 "Test layout" specified "pytest + pytest-asyncio + freezegun". **§8 fix in 04 v1:** §2 now uses `unittest` + `unittest.IsolatedAsyncioTestCase` + `tempfile.TemporaryDirectory` (per §9 `with` rule) + `unittest.mock.patch` (no pytest, no freezegun). §3 "Error path" tests that previously asserted "result is partial / no service" are replaced with `assertRaises` tests asserting the named exception propagates. **No spec divergence from 02 §3.1:** 02 §3.1 behavior is at the MCP server / FastMCP framework boundary, not the tool-function level. The 03 v1 + 04 v1 spec is consistent with 02 §3.1 (the boundary handler is outside this module's design). **05 v1 amend** added: "Test framework" column to §1 module table (both modules: `unittest`); new risk row in §4 documenting the 03 / 04 v0 violations and their mitigation; Done criteria §5 now calls `python -m unittest discover` instead of `pytest -q`. **Reviewer follow-up:** 04 R1 review was in flight at amend time (`agent:main:subagent:d9f8d6a0-6d28-4a9d-a775-7dc57f4b276c`, runId `ac3dbd72-f97a-4686-8315-975c59d580d3`). The R1 verdict was against v0 spec (pytest); the v1 amend is a **major structural change** (test framework switch + 10+ test name changes from "result is partial" to "raises X"). **Owner decision required:** (a) accept v0 R1 verdict as "structural findings" and re-spawn R1 against v1; (b) re-spawn R1 immediately to save round trips; (c) treat v0 R1 as null and self-PASS the v1 amend. Default recommendation: **(b) re-spawn R1** — the v1 amend is a clean re-write of §2 + parts of §3, and the original R1's MINORs are almost certainly stale. |
| 2026-07-01T01:25:30+08:00 | — | `mcp_server/04-test-design.md` v2 → v3 (owner restructure: 1 unit file → 6 test files + 1 mixins module under `unit/mcp_server/`) | (pending R3) | Owner direction (2026-07-01T01:25): "把可以，但是把mcp server相关的测试文件放在unit/mcp_server文件夹里." Motivation: v2 §2 places all 130 unit tests in a single `tests/unit/test_mcp_server.py` file. With §3.4 `invoke_model` alone at 30 tests, the single file would be ~3000+ lines and unwieldy. Owner accepted the v2 amend but added a directory-nesting requirement. **Layout-only amend** (no test count, no test name, no spec-content change from v2): (a) §2 directory tree rewritten — `tests/unit/test_mcp_server.py` → `tests/unit/mcp_server/{_mixins.py, test_list_local_models.py, test_start_service.py, test_list_running_services.py, test_invoke_model.py, test_release_service.py, test_helpers.py}`. (b) §2 single-file class list replaced with a per-file class / test-count / bucket table (5 public tools → 5 files; 11 internal helpers → 1 consolidated `test_helpers.py`; shared `_MockPopenMixin` hoisted to `_mixins.py`). (c) §3.1–§3.5 / §4.1–§4.11 headings annotated with `→ unit/mcp_server/test_xxx.py` / `→ unit/mcp_server/test_helpers.py (§4.N)`. (d) Conformance notes: appended "v3 amend" entry summarizing the restructure + key rationale (one file per public tool; `_mixins.py` as standard test-scaffolding location; `invoke_model`'s 30 tests kept in one file because they target one public function). **Test count preserved:** §3.1=14, §3.2=20, §3.3=10, §3.4=30, §3.5=11, §4.1–§4.11=43 (total 130). **R2 verdict impact:** R2 had reported one outstanding R1 v0 MAJOR — §3.3 `list_running_services` Edge bucket has 2 tests where ≥3 is required. That MAJOR is **deliberately NOT addressed in v3** (owner direction was to restructure first, address bucket-size in a follow-up amend). v3 is layout-only; the bucket-size gap remains. File size: 331 → 349 lines (+18 = directory tree + file-class table + 16 file-path annotations on §-headings + 1 new "v3 amend" Conformance notes block). **Pending decision (Gate 2 / agent):** (a) spawn R3 review against v3 to confirm the layout-only amend is clean and the v0 MAJOR is still the only outstanding issue; (b) first amend v3 → v4 to fix the §3.3 Edge bucket MAJOR (add a 3rd Edge test) so R3 has a clean PASS target; (c) accept v3 as-is and address the bucket gap at the 05/06 implementation stage. Default recommendation: **(a) spawn R3** first — the restructure is independent of the bucket-size gap and a clean R3 PASS makes the v4 amend a 1-issue amend instead of a mixed change.
| 2026-07-01T01:35:30+08:00 | — | `dev-doc/05-dev-plan.md` v1 → v2 (owner restructure: reflect 04 v3 layout in module 2 + risks) | (agent self-PASS, no R2 spawned) | Owner direction (2026-07-01T01:34): "pass，继续写完05-dev-plan". Mirrors 04 v3 layout amend (2026-07-01T01:25) into the dev plan. (a) Conformance notes: appended "v2 amend" entry summarizing the 04 v3 restructure. (b) §1 table row 2 doc-status: "04 v1 (R1 verdict deferred until post-amend re-review)" → "04 v3 (layout amend; R3 pending)". (c) §2 Module 2 exit criteria: `tests/unit/test_mcp_server.py` (unit) → `tests/unit/mcp_server/` (unit, per v3 layout) + inline tree showing the 6 test files + `_mixins.py` + per-file test count. (d) §3 Module 2 review log: replaced 1 unit-test entry with 7 entries (`_mixins.py` + 6 test files). (e) §4 risks: added 2 new rows — (i) §3.3 Edge bucket MAJOR deferred to 04 v4; (ii) 04 v3 split-boundary could mis-draw, duplicating setup. **Test framework column, module order, exit criteria shape, done criteria, §6 dev-doc-driven convention block all unchanged from v1.** File size: 78 → 103 lines (+25 = new risks + tree + log entries + Conformance notes v2 amend). **Agent self-PASS rationale:** (a) v1 already cleared R1 with PASS + 0 issues + 0 MINOR; v2 is a structural reflect of an already-R1-cleared 04 layout amend, not a design change; (b) no spec-content moved; (c) re-spawning R2 to verify a tree-text + risk-row edit is overhead with no signal. **Gate 2 (human) scope for this artifact:** (1) ship 05 v2 as-is and proceed to module 1 implementation (cache_resolver source + tests); (2) ship 05 v2 with the §3.3 Edge bucket gap added to §4 risks (already done); (3) reject and re-draft 05 to defer mcp_server details to a separate 05 file (not recommended — 05 is the source-of-truth for both modules).
